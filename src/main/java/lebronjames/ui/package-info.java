/**
 * Everything the user sees and types.
 *
 * <p>{@link lebronjames.ui.Ui} owns the input reader and every message the
 * chatbot prints, so no other class calls {@code System.out} directly and the
 * wording of a reply can be changed in one place.
 */
package lebronjames.ui;
