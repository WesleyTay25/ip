/**
 * Reading the task list from, and writing it to, a file on the hard disk.
 *
 * <p>{@link lebronjames.storage.Storage} is the only class that knows the
 * layout of the save file, so the format can change without affecting the rest
 * of the application.
 */
package lebronjames.storage;
